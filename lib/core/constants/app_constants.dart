class AppConstants {
  static const String appName = 'Neeti Flow CRM';
  
  // Firebase Collections
  static const String usersCollection = 'users';
  static const String customersCollection = 'customers';
  static const String dealsCollection = 'deals';
  
  // API Endpoints (for future migration)
  static const String baseUrl = '';  // Will be used when migrating to custom backend
}
