/// Enum for Lead Status
enum LeadStatus {
  warm,
  cold,
  hot
}

/// Enum for Lead Process Status
enum ProcessStatus {
  fresh,
  inProgress,
  completed,
  rejected
}

/// Enum for Leads Management Status
enum LeadsStatus {
  initial,
  loading,
  success,
  failure
}
